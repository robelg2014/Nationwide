console.log("Working")

let bottomContainer = $("<div>").attr("id",("bottom-container"))
$("body").append(bottomContainer)
let topContainer = $("<div>").attr("id", ("top-container"))
$("body").append(topContainer)
